# انسخ هذا الملف إلى .env.local (لا يُرفع لـ git — *.local مستثناة في .gitignore)
# واملأ القيمتين من: مشروعك على supabase.com → Settings → API
# بعد الإنشاء أو التعديل، أعد تشغيل خادم التطوير (npm run dev)

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
